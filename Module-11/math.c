#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int x;
    scanf("%d", &x);

    // ceil
    // int result = ceil(x);
    // printf("%d", result);

    // power
    // int a, b;
    // scanf("%d %d", &a, &b);
    // int result = pow(a, b);
    // printf("%d", result);

    // square
    // int result = sqrt(x);

    // printf("%d", result);

    // absolute value;
    int result = abs(x);
    printf("%d", result);
    return 0;
}