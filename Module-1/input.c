#include <stdio.h>

int main()
{
    // int num1;
    // float f_num;
    // char c_text;
    // scanf("%d %f %c",&num1,&f_num,&c_text);
    // printf("%d %.1f %c",num1,f_num,c_text);

    // int num,num2;
    // char p;
    // scanf("%d%c %d%c",&num,&p,&num2,&p);
    // printf("%d%% %d%%",num,num2);
    int num, num2;
    scanf("%da %db", &num, &num2);
    printf("%da %db", num, num2);
    return 0;
}